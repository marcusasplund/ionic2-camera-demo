#
# Configuration
#

sudo: required
dist: trusty

branches:
  only:
    - master

#
# Build Lifecycle:
#

before_install:
  - nvm install node
  - nvm use node
  - npm install -g cordova ionic
  - ionic state clear

install: npm install

